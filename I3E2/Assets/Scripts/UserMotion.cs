using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UserMotionl : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if(Input.GetKey(KeyCode.A)){   //왼팔

        }
        if(Input.GetKey(KeyCode.D)){  //오른팔
        	
        }
        if(Input.GetKey(KeyCode.LeftArrow)){  //왼다리
        	
        }
        if(Input.GetKey(KeyCode.RightArrow)){  //오른다리
        	
        }
    }
}
